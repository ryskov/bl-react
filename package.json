{
  "name": "bl-react",
  "version": "0.0.1",
  "private": true,
  "main": "./bundle.min.js",
  "scripts": {
    "build": "rm -rf lib && babel src -d lib && cp -r src/styles lib/"
  },
  "dependencies": {
    "react": "^15.2.1",
    "underscore": "^1.8.3"
  },
  "devDependencies": {
    "babel-cli": "^6.16.0",
    "babel-preset-es2015": "^6.16.0",
    "babel-preset-react": "^6.16.0",
    "babelify": "^7.3.0",
    "browserify-css": "^0.9.2"
  }
}
